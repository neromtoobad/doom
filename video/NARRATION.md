# Narration

Read in the project owner's cloned voice, tempo-lifted 1.12x, silence-trimmed and
normalised to -17 LUFS. Cues are computed from the measured audio by
`capture/plan.mjs`, which refuses any line that would run past the scene it belongs
to — the numbers below are its output, not an estimate.

| cue | scene | line |
|---|---|---|
| `0:01.00` | Title | Doom. A prediction market where the odds are public and the bettors are not. |
| `0:09.00` | The problem | Prediction markets work because you can see the money. That is also the problem. |
| `0:17.50` |  | Wallets are public, so the crowd follows the biggest bettor instead of the evidence. |
| `0:28.50` | The book | So here is the leaderboard. Every position, largest first, from the contract. |
| `0:37.00` |  | Every size is there. The name column is drawn, and empty. A position is keyed by a secret, and the pool made the call. |
| `0:50.50` | The market maker | Underneath is a real market maker. The price is a probability, and every buy moves it. |
| `1:00.00` |  | Bigger orders pay worse, and the panel says so before you commit. |
| `1:08.50` | What it costs | Now the part most demos leave out. The pool charges six tokens for every private operation. |
| `1:19.81` |  | On a one token bet that is fatal. You get one point seven six back, having spent seven. |
| `1:29.50` |  | So Doom tells you not to take the trade. Stake forty and it clears the fee. |
| `1:39.50` | Open a market | Anyone can open a market. Pick an asset, a direction, a strike, and the question writes itself. |
| `1:50.00` |  | The class is already declared, so this is a deploy. Two signatures, no terminal. |
| `1:59.50` | Settlement | For price questions the panel reads Pragma and fills in the proposal. |
| `2:08.50` |  | Settlement stays a bonded human claim. The oracle informs it, and we say so. |
| `2:19.00` | Your keys | Positions come from your wallet, not this browser. Sign once, and the keys rebuild anywhere. |
| `2:31.00` | Proof | Four transactions on mainnet. Four different senders, none of them the bettor. |
| `2:41.00` |  | Fourteen contracts. Ninety five tests. One command that checks every claim against the chain. |
| `2:52.50` | Close | Doom. Visible odds. Invisible bettors. |

18 lines · 129s of speech in 180s (71% density) · ends at 174.7s
